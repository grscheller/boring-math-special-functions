[build-system]
requires = ["flit_core>=3.12,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "boring-math-special-functions"
version = "5.1.0"
readme = "README.md"
requires-python = ">=3.12"  # syntactic floor: known to SyntaxError below this
authors = [
  { name = "Geoffrey R. Scheller", email = "geoffrey@scheller.com" },
]
keywords = [
  "special functions",
  "sin",
  "cos",
  "tan",
  "exponential",
  "gamma",
  "beta",
]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Framework :: Pytest",
  "Intended Audience :: Education",
  "License :: OSI Approved :: Apache Software License",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3.14",  # only versions which have past pytest
  "Typing :: Typed",
]
dependencies = []
description = "Special functions for Mathematics and Physics."

[project.urls]
Changelog = "https://github.com/grscheller/boring-math-special-functions/blob/main/CHANGELOG.md"
Documentation ="https://grscheller.github.io/boring-math-special-functions/release/html/"
Homepage = "https://grscheller.github.io/boring-math/homepage/build/html/"
Source = "https://github.com/grscheller/boring-math-special-functions"

[project.optional-dependencies]
test = [
  "pytest>=8.4.1",
]

[tool.flit.module]
name = "boring_math.special_functions"

[tool.mypy]
explicit_package_bases = true
local_partial_types = true
namespace_packages = true
warn_redundant_casts = true
warn_return_any = true
warn_unused_configs = true

[tool.pytest]
addopts = ["-ra"]
consider_namespace_packages = true
testpaths = ["tests/"]

[tool.ruff]
target-version = "py314"

[tool.ruff.lint.flake8-quotes]
docstring-quotes = "double"

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = [
  "E731",
  "C901",
]
"**/*.py" = ["E741"]

[tool.ruff.format]
quote-style = "single"
docstring-code-line-length = 72
